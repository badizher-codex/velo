<Window x:Class="VELO.UI.Dialogs.BookmarksWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VELO — Marcadores"
        Width="560" Height="520"
        MinWidth="420" MinHeight="320"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">

    <!-- Phase 5.1 re-skin — follows the History frame pattern. -->
    <Grid Background="{DynamicResource SurfaceCanvasBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="22,20,22,0">
            <TextBlock x:Name="HeaderLabel" FontSize="22" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}"/>
        </Grid>

        <!-- Search -->
        <Border Grid.Row="1" Margin="22,14,22,0"
                Background="{DynamicResource SurfaceOverlayBrush}"
                BorderBrush="{DynamicResource BorderSubtleBrush}"
                BorderThickness="1" CornerRadius="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="🔍" FontSize="14"
                           VerticalAlignment="Center" Margin="14,0,8,0"
                           Foreground="{DynamicResource TextMutedBrush}"/>
                <TextBox x:Name="SearchBox" Grid.Column="1"
                         Background="Transparent" BorderThickness="0"
                         Foreground="{DynamicResource TextPrimaryBrush}"
                         CaretBrush="{DynamicResource AccentBrush}"
                         SelectionBrush="{DynamicResource AccentBrush}"
                         Padding="0,11,12,11" FontSize="13"
                         TextChanged="Search_Changed"/>
            </Grid>
        </Border>

        <!-- List -->
        <ScrollViewer Grid.Row="2" Margin="22,14,22,16"
                      VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="BookmarkList"/>
        </ScrollViewer>
    </Grid>
</Window>
